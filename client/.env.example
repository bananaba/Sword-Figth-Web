# Cloudflare Worker base URL (Phase 11+ ranked backend).
# Dev (wrangler dev): http://localhost:8787
# Prod: https://chambara-ranked-worker.<your-subdomain>.workers.dev
VITE_WORKER_URL=http://localhost:8787
